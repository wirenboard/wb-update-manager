#compdef wb-release

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _wb-release (e.g. ~/.local/share/zsh_completion/_wb-release).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_wb_commands() {
  local _commands=(
    
  )
  _describe 'wb-release commands' _commands
}

_shtab_wb_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-r,--regenerate}"[regenerate factory sources.list and exit]"
  {-t,--target-release}"[upgrade release to a new target (stable or testing)]:target_release:"
  {-v,--version}"[print version info and exit]"
  {-y,--yes}"[auto \"yes\" to all questions]"
  {-p,--reset-packages}"[reset all packages to release versions and exit]"
  {-l,--log-filename}"[path to output log file]:log_filename:"
  "--no-journald-log[disable journald logging]"
  "--reset-url[reset repository URL to default Wirenboard one]"
  "--prefix[override repository URL prefix]:prefix:"
  "--no-preliminary-update[skip upgrade before switching (not recommended)]"
)

# guard to ensure default positional specs are added only once per session
_shtab_wb_defaults_added=0


_shtab_wb() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->wb-release'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_wb_defaults_added )); then
    if (( ${_shtab_wb_options[(I)${(q)one_or_more}*]} +          ${_shtab_wb_options[(I)${(q)remainder}*]} +          ${_shtab_wb_options[(I)${(q)default}]} == 0 )); then
      _shtab_wb_options+=(': :_shtab_wb_commands' '*::: :->wb-release')
    fi
    _shtab_wb_defaults_added=1
  fi
  _arguments -C -s $_shtab_wb_options

  case $state in
    wb-release)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_wb-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_wb -N wb-release
else
  # autoload from fpath, call function directly
  _shtab_wb "$@"
fi

